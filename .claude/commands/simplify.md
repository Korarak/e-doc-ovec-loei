Review the code changed in this session (or the files I specify) for opportunities to improve reuse, quality, and efficiency. Then fix any issues found.

Focus on:
- Dead code or redundant logic that can be removed
- Repeated patterns that should be extracted into a shared utility or hook
- Overly complex expressions that can be simplified
- Unnecessary state, variables, or computations
- Anything that violates the existing conventions visible in surrounding code

Do NOT:
- Add new features or change behavior
- Add comments, docstrings, or type annotations to code you didn't touch
- Refactor code that wasn't part of the changes
- Apply speculative abstractions for hypothetical future use

Make the changes directly. Report what you changed and why in one or two sentences per change.
