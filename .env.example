MYSQL_HOST=db
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=e-sign
MYSQL_USER=esign
MYSQL_PASSWORD=your_user_password

# Host ports (defaults shown; change if they collide with other services)
APP_PORT=8080
DB_PORT=3306
PMA_PORT=8081

# Gemini AI (optional — required only when using Gemini mode in doc_add.php)
GEMINI_API_KEY=your_gemini_api_key
# GEMINI_MODEL=gemini-2.5-flash-preview-05-20
